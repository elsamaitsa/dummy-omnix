{
  "name": "dashboard",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "pages/*"
  ],
  "scripts": {
    "start": "lerna run start --stream --parallel",
    "build": "lerna run build --stream --parallel"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
